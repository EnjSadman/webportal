export enum API_CALL_DESTINATION {
  nasa = 'https://api.nasa.gov/',
}

export enum API_CALL_METHOD {
  get = 'GET',
  post = 'POST',
  put = 'PUT',
  delete = 'DELETE',
  patch = 'PATCH',
}

export enum NASA_CALL_TYPE {
  apod = 'planetary/apod',
}

export enum NAV_LINK_TYPE {
  header,
  footer,
  regular,
}
